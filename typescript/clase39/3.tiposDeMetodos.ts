let persona = {
    nombre:"Paula",
    edad: 25,
    pais: "Argentina"
};

//Claves del objeto - Da los nombres de las propiedades del objeto
const keys = Object.keys(persona);
console.log(`Claves del objeto: ${keys}`);

//Valores del objeto
const values = Object.values(persona);
console.log("Valores del objeto", values);


