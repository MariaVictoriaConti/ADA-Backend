const prompt = require("prompt-sync")({ sigint: true});

let primerNumero = 20;
let segundoNumero = 10;

let resultadoSuma = console.log(primerNumero + segundoNumero);

let resultadoResta = console.log(primerNumero - segundoNumero);

let resultadoMultiplicacion = console.log(primerNumero * segundoNumero);

let resultadoDivision = console.log(primerNumero / segundoNumero);

